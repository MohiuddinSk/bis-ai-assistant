import { useState } from 'react'; import type { Citation } from '../types/chat';
export function CitationCard({citation}:{citation:Citation}){const [open,setOpen]=useState(false); const pages=citation.page_start===citation.page_end?`Page ${citation.page_start??'not available'}`:`Pages ${citation.page_start??'?'}–${citation.page_end??'?'}`; return <article className="citation"><button aria-expanded={open} onClick={()=>setOpen(!open)}>{citation.citation_id} · {citation.source_filename??'Indexed document'} · {pages}</button>{open&&<div><p>{citation.excerpt}</p><details><summary>Technical details</summary><code>{citation.chunk_id}</code></details></div>}</article>}
