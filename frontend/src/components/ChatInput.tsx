import { useState } from 'react';
export function ChatInput({onSend,busy}:{onSend:(q:string)=>void;busy:boolean}){const [value,setValue]=useState(''); const submit=()=>{const q=value.trim();if(q&&!busy){onSend(q);setValue('')}}; return <div className="input-wrap"><label htmlFor="question">Ask a question about BIS toy standards</label><textarea id="question" value={value} maxLength={1000} disabled={busy} onChange={e=>setValue(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();submit()}}}/><div><small>{value.length}/1000</small><button onClick={submit} disabled={busy||!value.trim()}>{busy?'Sending…':'Send'}</button></div></div>}
