import { useState } from 'react';
import type { Audience } from '../types/chat';
export function ChatInput({onSend,busy,audience,onAudienceChange}:{onSend:(q:string)=>void;busy:boolean;audience:Audience;onAudienceChange:(audience:Audience)=>void}){const [value,setValue]=useState(''); const submit=()=>{const q=value.trim();if(q&&!busy){onSend(q);setValue('')}}; return <div className="input-wrap"><label htmlFor="audience">I am a:</label><select id="audience" value={audience} disabled={busy} onChange={event=>onAudienceChange(event.target.value as Audience)}><option value="general">General user</option><option value="manufacturer">Manufacturer</option><option value="consumer">Consumer</option></select><label htmlFor="question">Ask a question about BIS toy standards</label><textarea id="question" value={value} maxLength={1000} disabled={busy} onChange={e=>setValue(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();submit()}}}/><div><small>{value.length}/1000</small><button onClick={submit} disabled={busy||!value.trim()}>{busy?'Sending…':'Send'}</button></div></div>}
