import type { ChatResponse } from '../types/chat'; import { CitationCard } from './CitationCard';
export function ChatMessage({role,text,response}:{role:'user'|'assistant';text:string;response?:ChatResponse}){return <article className={`message ${role}`}>{role==='assistant'&&response&&<span className={`answer-status ${response.insufficient_evidence?'insufficient':'grounded'}`}>{response.insufficient_evidence?'Evidence insufficient':'Grounded answer'}</span>}<p>{text}</p>{response?.citations.map(c=><CitationCard key={`${c.citation_id}-${c.chunk_id}`} citation={c}/>)}</article>}
